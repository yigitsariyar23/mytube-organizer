# Personal dev utilities / data dumps — not part of the shipped extension.
# (Kept locally, just untracked. See CLAUDE.md "Cruft".)
scraped-ids.txt
csv-ids.txt
check-missing.sh

# Written by native-host/install.sh on Windows: which Python launcher exists and
# where this checkout sits are per-machine facts, so neither file is shared.
native-host/mytube-updater.bat
native-host/com.mytube.organizer.updater.json

# OS / editor noise
.DS_Store

# Claude Code transient worktrees
.claude/worktrees/
